export workingdir=${HOME}/qualification/bJetTrigger
export date=20190308 #the date in YYYYMMDD
export size=med #sample size specifier, e.g. med, large, full: these refer to pre-defined samples YOU created

#comment one of these two out if not doing hybrid tuning
export ttbar=ttbar_41000 #ttbar sample, e.g. ttbar_41000
#export zprime=Zprime_427080 #Zprime sample, e.g. Zprime_427080
